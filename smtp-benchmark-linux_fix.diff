--- smtpsend/smtpsend.c	2006-06-24 13:22:47.000000000 +0200
+++ smtpsend/smtpsend.c	2026-08-03 04:40:08.020242209 +0200
@@ -35,6 +35,7 @@
 #include <stdio.h>
 #include <stdlib.h>
 #include <string.h>
+#include <time.h>
 #include <unistd.h>
 
 #define TIMEOUT 60
@@ -135,25 +136,25 @@
 	int verbosity;
 	int size;
 
-	strlcpy(buf, "MAIL FROM:<", sizeof(buf));
-	strlcat(buf, from, sizeof(buf));
-	strlcat(buf, ">", sizeof(buf));
+	strcpy(buf, "MAIL FROM:<");
+	strcat(buf, from);
+	strcat(buf, ">");
 
 	if (smtp_send(fp, buf) != '2') {
 		warnx("protocol error, MAIL FROM");
 		return -1;
 	}
 
-	strlcpy(buf, "RCPT TO:<", sizeof(buf));
-	strlcat(buf, to, sizeof(buf));
-	strlcat(buf, ">", sizeof(buf));
+	strcpy(buf, "RCPT TO:<");
+	strcat(buf, to);
+	strcat(buf, ">");
 
 	if (smtp_send(fp, buf) != '2') {
 		warnx("protocol error, RCPT TO");
 		return -1;
 	}
 
-	strlcpy(buf, "DATA", sizeof(buf));
+	strcpy(buf, "DATA");
 
 	if (smtp_send(fp, buf) != '3') {
 		warnx("protocol error, DATA");
@@ -162,16 +163,16 @@
 
 	verbosity = verbose;
 
-	strlcpy(buf, "To: ", sizeof(buf));
-	strlcat(buf, to, sizeof(buf));
+	strcpy(buf, "To: ");
+	strcat(buf, to);
 	send_line(fp, buf);
 
-	strlcpy(buf, "From: ", sizeof(buf));
-	strlcat(buf, from, sizeof(buf));
+	strcpy(buf, "From: ");
+	strcat(buf, from);
 	send_line(fp, buf);
 
-	strlcpy(buf, "Subject: ", sizeof(buf));
-	strlcat(buf, subject, sizeof(buf));
+	strcpy(buf, "Subject: ");
+	strcat(buf, subject);
 	send_line(fp, buf);
 	send_line(fp, "X-Mailer: smtpsend\r\n");
 
@@ -179,7 +180,7 @@
 	 * Change the following message generator to something more
 	 * intelligent some day...
 	 */
-	strlcpy(buf, "ABCDEFGHIJKLMNOPQRSTUVWXYZ1234567890", sizeof(buf));
+	strcpy(buf, "ABCDEFGHIJKLMNOPQRSTUVWXYZ1234567890");
 
 	for (size = nbytes; size > 0; size -= 36)
 		send_line(fp, buf);
